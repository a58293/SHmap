# v0.8.7 详情页统一顺序

- 独立地图对象与地块内部资料改用同一个详情页排序器。
- 所有详情统一为：顶部摘要 → 本格资料主题 → 所属地块档案 → 资料身份 → 条目特征 → 其他详细内容。
- `09.详细描述`统一提升到“所属地块档案”中，不再因对象类型或是否导入 Markdown 而落到页面底部。
- 不修改数据库、基础对象、坐标、水系与关系。

# v0.8.6 所属地块档案顺序修正

- 内部条目统一按“顶部摘要、本格资料主题、所属地块档案、资料身份、条目特征、其他内容”显示。
- “所属地块档案”不再位于内部条目详情的后部。
- 独立地图对象继续先显示对应的“地块身份与位置”。
- 不修改 `data.js`、SQLite 数据库、坐标、对象数量、水系或关系。

# v0.8.5 本格资料主题上移

- “本格资料主题”统一放在顶部摘要之后、资料身份之前。
- 独立地图对象和地块内部 Markdown 条目采用相同顺序。
- 详细资料仍完整显示在主题切换栏之后，不再把主题栏放到页面底部。
- 不修改 `data.js`、SQLite 数据库、坐标、对象数量、水系或关系。

# v0.8.4 详细资料优先显示

- 所有新版详情页统一先显示详细资料，再在底部显示“本格资料主题”。
- 独立地图对象与地块内部 Markdown 条目采用相同顺序。
- 本格主题切换功能保留，仅调整阅读顺序。
- 不修改 `data.js`、SQLite 数据库、坐标、对象数量、水系或关系。

# v0.8.3 独立对象与内部条目详情界面统一

- 独立地图对象与地块内部 Markdown 条目统一使用规整的宽版详情布局。
- 内部条目按“资料身份、条目特征、原文与来源、所属地块档案”分区显示，不再使用旧式纵向字段清单。
- 内部条目保留“不是独立地图对象”的资料属性，不新增坐标、地块或对象。
- 两类详情均提供地图定位、打开完整博物志和同格资料主题切换入口。
- 不修改 `data.js`、SQLite 数据库、617 条历史资料、591 个独立地图对象、79 段水系或 118 个箭头格。

# v0.8.2 对象详情整合地块档案

- 图一对象抽屉扩展为适合阅读的宽版双栏布局。
- 将图二信息按“地块身份与位置、对象特征、原文与注疏、研究与定位、Markdown完整档案”规整分区。
- 增加摘要卡、本地标签、同格资料主题切换、地图定位和打开完整博物志入口。
- 空字段自动隐藏，避免重新形成冗长的纵向字段列表。
- 不修改数据、坐标、对象数量、水系或关系。

# v0.8.1 同格多主题 Markdown 完整展示

- 同一100里主格内存在多份 Markdown 时，每份继续绑定各自目标对象，不互相覆盖或合并。
- 博物志新增“本格资料主题”切换栏，可在都广之野、后稷葬等同格主题之间直接切换。
- 左侧格内对象明确标注哪些对象具有完整 Markdown 档案。
- 对象详情抽屉完整显示九段式的01—09节，第09节“详细描述”不再隐藏。
- “资料来源”页单独显示第07节原文摘录和第08节其他典故。
- 不改变坐标、对象数量、地块结构、关系线或数据库基础资料。

# v0.8.0 博物志条目规范化与重复清理

- 第06节的“备注／说明／补充说明”作为条目字段保存，不再生成独立卡片。
- 统一清除条目名称中的 Markdown 粗体、列表编号和句末标点。
- 显示时自动合并 `**氐人**。`、`氐人。` 等同名变体，并将旧备注卡片归回所属条目。
- 重新导入同一资料时，将规范化结果写回博物志，清理旧版解析器遗留的重复卡片。
- 保留 v0.7.9 的“删除本轮修改”入口、自动备份和倒序恢复。
- 删除前自动建立桌面数据库备份，不删除既有历史备份。
- 支持撤销超过普通40条撤销栈上限的大批量 Markdown 导入。
- 修复第06节字段行被误识别为博物志卡片的问题，并过滤旧占位条目。
- 修复左侧搜索结果浅色卡片文字对比度，并移除索引栏底部装饰图。
- package、Tauri、Cargo、VERSION、运行时与验证脚本统一为 0.8.0。

# v0.7.7 集合对象与资料角色分层

- 617条历史资料记录保持不删，新增 `entity / collection / subregion / path / detail / context` 六类角色。
- 591条独立地图对象继续进入100里主格；26条集合、路径、内部资料和上下文记录不再额外占格。
- 九丘改为九个具名丘陵的父级集合，只绘制整体范围并提供九个成员入口。
- 岷三江、帝台集合、帝下两坛改为集合档案；水系共线路径只绘制线路。
- 昆仑九井、昆仑九门、幽都五兽等进入所属主体资料层。
- 角色匹配使用稳定语义键并保留行号快照，不依赖会变化的运行时对象ID。

# v0.7.6 安全导入正式版本

- 安全导入识别和语义回退由补丁提升为正式版本。
- 活动验证入口切换为verify-v0.7.6系列。
- 基础数据、坐标、水系、关系和对象数量不变。

# v0.7.5 版本同步与安全导入修正

- 正式项目版本由 0.7.4 升至 0.7.5。
- 验证脚本入口改为 verify-v0.7.5 系列。
- 保留历史 v0.7.4 功能记录，不将旧记录伪改为新版本。

# v0.7.4 原文直载关系显示精简

- Markdown关系字段为“原文直载（葬／有／居／经等）”时，博物志卡片与对象详情显示为“原文”。
- 原始导入数据不改写；导入检查器继续显示原字段。
- 其他关系说明保持原样。

# v0.7.4 卡片出处精简与审计信息下沉

- 卡片第三项统一为“出自《某某经》”。
- G3/G4、坐标性质、锁定状态等审计文字移入对象详细资料。
- 长关系／特征文本限制在卡片范围内，避免覆盖下一张卡片。
- 经篇名称保持可点击并跳转目录。

# SHmap v0.7.4

## Markdown博物志条目展示修正

- 没有实际条目的分类栏不再显示，不再占据空白列。
- 九段式条目卡片只读取Markdown第06节的原始字段，不再使用对象母表中的核心特征、证据等级或坐标审计文字替代。
- 卡片按顺序显示：1.与本地关系、2.核心特征、3.证据、4.功效；没有填写的字段自动隐藏。
- 《南山经》《大荒南经》以及《山海经·大荒南经》等经篇名称可点击，直接打开左侧对应经篇对象目录并同步过滤地图。
- 典籍出处、原文摘录和资料证据链中的经篇名称也支持同样跳转。
- 保留安全补充导入、通用父子地点层级、启动卡死恢复与全部地图数据。

基础数据保持：617个对象、79段水系、118个箭头格。

## 0.7.4 - 博物志专名别名与类型安全绑定

- 修复九疑山／九嶷山异名绑定。
- 条目匹配增加对象分类校验。
- 旧未绑定条目在显示时动态重新解析。
- 帝舜等人物条目不再被帝舜台等地点对象干扰。
