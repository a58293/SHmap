import fs from "node:fs";import path from "node:path";
const root=process.cwd();const required=["index.html","src/desktop-bootstrap.js","public/app/app.js","public/app/data.js","src-tauri/src/lib.rs","src-tauri/tauri.conf.json","src-tauri/tauri.release.conf.json",".npmrc"];
let failed=false;for(const file of required){if(!fs.existsSync(path.join(root,file))){console.error(`缺少：${file}`);failed=true}}
const data=fs.readFileSync(path.join(root,"public/app/data.js"),"utf8");const marker="window.SHJ_INITIAL_DATA=";const start=data.indexOf(marker)+marker.length;let depth=0,inString=false,escape=false,end=-1;for(let i=start;i<data.length;i++){const c=data[i];if(inString){if(escape)escape=false;else if(c==="\\")escape=true;else if(c==='"')inString=false;continue}if(c==='"'){inString=true;continue}if(c==='{')depth++;if(c==='}'){depth--;if(depth===0){end=i+1;break}}}const initial=JSON.parse(data.slice(start,end));if(initial.objects.length!==395){console.error(`对象数量异常：${initial.objects.length}`);failed=true}const ids=new Set(initial.objects.map(x=>x.id));if(ids.size!==initial.objects.length){console.error("对象ID重复");failed=true}
const pkg=JSON.parse(fs.readFileSync(path.join(root,"package.json"),"utf8"));const conf=JSON.parse(fs.readFileSync(path.join(root,"src-tauri/tauri.conf.json"),"utf8"));const cargo=fs.readFileSync(path.join(root,"src-tauri/Cargo.toml"),"utf8");if(pkg.version!=="0.2.0"||conf.version!=="0.2.0"||!/^version = "0\.2\.0"$/m.test(cargo)){console.error("v002版本号未同步");failed=true}if(!conf.plugins?.updater?.pubkey||!conf.plugins.updater.endpoints?.[0]?.includes("a58293/SHmap/releases/latest/download/latest.json")){console.error("更新器配置缺失");failed=true}if(!cargo.includes('tauri-plugin-updater = "2"')){console.error("Rust更新插件缺失");failed=true}
for(const name of fs.readdirSync(root)){if(/\.key$|PRIVATE_KEY|password/i.test(name)){console.error(`仓库根目录疑似包含密钥：${name}`);failed=true}}
const lock=fs.readFileSync(path.join(root,"package-lock.json"),"utf8");if(lock.includes("applied-caas-gateway")||lock.includes("artifactory/api/npm")){console.error("package-lock仍包含内部依赖地址");failed=true}
console.log(`校验：${initial.objects.length}个对象，程序v002 / 0.2.0，数据版本${initial.metadata.dataVersion}`);if(failed)process.exit(1);
