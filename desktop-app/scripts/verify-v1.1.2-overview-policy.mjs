import fs from "node:fs";import assert from "node:assert/strict";const app=fs.readFileSync("public/app/app.js","utf8");assert.ok(app.includes("showDetails=overviewZoom>=.14"));assert.ok(app.includes("showMacroLabels=overviewZoom<.14"));assert.ok(app.includes("detailDomain=state.overviewMode===\"domain\"&&!group.macroRegion"));assert.ok(app.includes("const title=`${modeLabel} · ${count}`"));assert.ok(app.includes("zoomBand=zoom<.14?\"macro\":zoom<.52?\"detail\":\"evidence\""));console.log("PASS v1.1.2 overview policy.");
