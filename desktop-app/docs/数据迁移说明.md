# 数据迁移说明

桌面版和浏览器版使用不同的应用存储空间，无法自动读取Chrome或Edge网页中的localStorage。

桌面版v001已经直接内置v075母表的395个对象，因此不需要再次导入R197—R403增量包。

网页中另外做过的手工修改，可先在网页端使用“仅导出更改包”或导出Markdown，再在桌面版中导入。导入后点击“桌面备份”建立手动备份。

桌面版主数据库是 `shmap.db`。不要在程序运行时手工编辑该文件。需要复制数据时，应复制整个应用数据目录，或使用 `backups` 中的 `.shjbackup.json`。
