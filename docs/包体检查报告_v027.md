# SHmap v027 包体检查报告

## 已检查

- `app/app.js`：Node.js 语法检查通过。
- 单文件版内嵌主脚本：Node.js 语法检查通过。
- `VERSION.json`、`data/current.json`、`data/manifest.json` 等 JSON：解析通过。
- `app/index.html`：242 个 HTML ID，无重复 ID。
- `app/styles.css`、`app/app.js`、`app/data.js`：入口引用文件均存在。
- v027 核心函数：区域集合汇总、区域博物志渲染、选区定位均已写入。
- v027 核心样式：大字号对象卡片、单页区域博物志、采集地块目录均已写入。
- `app/data.js` 与 v026 原始运行数据 SHA256 一致，未改动 v031-r0001 地图对象数据。

## 本版修复

- 地块简述中的分类名、对象名、字段正文和证据文字整体放大。
- 画笔集合不再纵向排列多张被压缩的地块卡片。
- 所有采集地块合并为一页区域博物志。
- 六类对象、原文摘录、证据链、定位结论和待核对问题集中汇总。
- 保留采集地块目录，可逐个定位、打开完整博物志或移除。
