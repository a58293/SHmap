# SHmap v028 包体检查报告

- `app/app.js` 已通过 `node --check`。
- 单文件版内嵌脚本已分别提取并通过 `node --check`。
- 浏览器交互测试：画笔拖选后显示 `dossierWorkspace`，旧 `brushCollectionWorkspace` 保持隐藏。
- 浏览器交互测试：集合标题为“画笔采集 · 区域博物志”，并存在合并后的区域博物志卡牌。
- 浏览器交互测试：右键橙色已选地块后，集合数量从3减少到2。
- 地图数据版本保持 `v031-r0001`，未改动对象母表。
