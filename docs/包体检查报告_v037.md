# v037 包体检查报告

- JavaScript语法：通过（Node `--check`）。
- JSON解析：通过（VERSION、current、manifest、active-spec）。
- 分体版HTML静态ID：269个，重复ID为0。
- 单文件与分体版同步：通过，由最终分体版自动重新生成。
- 浏览器启动：通过，默认载入188个对象。
- v075增量导入：通过；一次识别并导入R197—R403共207项，导入后对象总数为395。
- 395对象交互：通过；连续拖动使用图层变换，松手后完成一次完整刷新，无脚本异常。
- 地图拖动：通过；拖动过程中复用原地块DOM并使用图层变换，松手后清除变换并完整刷新。
- 滚轮缩放：通过；连续缩放期间使用轻量变换，停止后恢复完整渲染。
- 红色画笔：通过；检测到实际Canvas笔迹并成功采集地块。
- 地图总览：通过；小地图点击产生跳转坐标，“地图总览”缩放到全部对象范围。
- 高倍率精细地图：通过；523%时进入精细地图并生成精确对象组。
- 浏览器脚本异常：0。
- 非预期控制台错误：0；离线测试中的GitHub网络请求失败属于预期情况。
- 数据版本：v031-r0001。
- 默认对象数：188。
- 本版数据改动：0。
