# 包体检查报告 v034

## 功能检查

- 42%及以下进入区域概览模式。
- 52%及以上恢复100里地块模式。
- 区域概览不绘制主格和10里格。
- 区域名称依据“所属区域／山系”、面积对象与对象密度聚合。
- 区域标签存在数量限制、重要度排序与屏幕碰撞避让。
- 点击区域标签可按区域范围计算目标缩放并居中。
- 悬停区域标签显示对象摘要。
- 地图研究数据保持 v031-r0001，共188个对象。

## 静态校验

- app.js 通过 Node 语法检查。
- 单文件内嵌 app 脚本通过 Node 语法检查。
- JSON 文件可正常解析。
- index.html 重复 ID：0。
- 分体版和单文件版均包含区域概览逻辑与样式。

## 说明

区域概览中的淡色范围是对象聚合后的导航提示，不修改原始面积、坐标、文本归属或证据等级。
