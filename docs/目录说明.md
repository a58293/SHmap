# 仓库目录说明

- `app/`：当前可运行的本地 Demo 和程序源文件。
- `data/`：数据版本清单；当前运行数据暂在 `app/data.js`。
- `source-workbooks/`：最新版 Excel 研究母表。
- `specs/`：地图研究方法与当前制图执行规则。
- `examples/`：Markdown 导入的正确案例、错误案例和空白模板。
- `docs/`：使用、上传与版本说明。
- `VERSION.json`：当前程序、数据和规格版本。
- `index.html`：以后启用 GitHub Pages 时可作为入口；本地也可直接打开。
