# 包体检查报告 v022

本次检查范围：程序脚本、HTML结构、JSON清单、Markdown解析案例、目录引用与版本信息。

## 已修复

1. Markdown 只能新增对象，不能更新“简要／基础／详细”等地块档案字段。
2. Markdown 不支持缩进多行文本。
3. “载入空白正确模板”实际载入了正确案例，而不是空白模板。
4. `app/index.html` 页面标题仍显示 v020。
5. `data/current.json` 的程序版本与更新摘要仍停留在 v020。
6. `CHANGELOG.md` 顶部版本与当前包体不一致。
7. `app/data.js` 中源母表文件名与仓库内实际文件名不一致。
8. 同一区块重复字段此前会静默覆盖；现在会提示并采用最后一次内容。
9. 同一 Markdown 内重复对象 ID 会被阻止。
10. 同一 Markdown 内重复地块档案会提示按文件顺序依次更新。

## 已校验

- `app/app.js` JavaScript 语法检查通过。
- `app/data.js` JavaScript 语法检查通过。
- 全部 JSON 文件可正常解析。
- HTML 页面不存在重复 ID。
- `app/index.html` 的本地 CSS、JS 引用文件均存在。
- 正确 Markdown 案例可识别为“对象＋地块档案”混合格式。
- 错误 Markdown 案例能识别出阻断错误。
- 地块档案多行文本、时间流逝、玩家可达性与经篇事件解析通过。

## 当前仍属原型限制

- GitHub 写入仍由 GitHub Desktop 完成。
- 正式数据发布包与 GitHub Actions 尚未自动生成。
- 浏览器本地存储仍是当前编辑工作区的主要持久化方式。
